# Social Engine Recovery 🛠️
**Data Vortex :: AARUUSH'26 — Round 1 Phase 1**
**Team:** SPARTANS

> **Mission**: The Social Engine suffered a critical failure, corrupting its data intake and analytical capabilities. This repository documents our complete recovery process — from extracting the hidden dataset from the crashed Node 07, through cleaning the corrupted pipeline artifacts, to advanced machine learning analysis.

---

## 📂 Repository Structure

When reviewing this submission, we recommend following our pipeline in this order:

### 1. The Full Narrative (Start Here)
* **`Social_Engine_Recovery.ipynb`** — Our complete, end-to-end Jupyter Notebook. We recommend viewing this first, as it contains our entire pipeline with all 20 advanced visualization plots rendered inline. 

### 2. The Final Report
* **`Social_Engine_EDA_Report_v2.pdf`** — A beautifully compiled PDF report containing all Exploratory Data Analysis and Advanced Analytics insights.

### 3. The Core Datasets
* **`Social_Engine_Posts_Corrupted.csv`** — The raw, corrupted data exactly as we extracted it from the React bundle. Included for data provenance.
* **`Social_Engine_Cleaned_Dataset.csv`** — The final, master dataset. We merged both the cleaned Posts and cleaned Users data into this single master file for easy modeling.

### 4. The Python Pipeline (Source Code)
* **`data_cleaning.py`** — The script we engineered to systematically fix the 8 unique data corruption patterns.
* **`eda_analysis.py`** — Generates the 13 foundational descriptive statistics plots.
* **`advanced_analysis.py`** — Our advanced suite featuring NLP Topic Modeling, Network Analysis, Random Forest Engagement Prediction, and K-Means User Segmentation.
* **`plots/`** — High-resolution renders of all 20 charts generated by our scripts.

---

## 🏆 Key Achievements & Insights

| Category | Finding |
|----------|---------|
| **Data Recovery** | Successfully extracted and cleaned 12,000 posts from a corrupted JS bundle, addressing 8 distinct corruption patterns (mojibake, negative values, HTML tags, etc.). |
| **Top Predictor** | Machine Learning feature importance (Random Forest) proved that **Follower Count** (16.9%) and **Shares** (16.7%) drive engagement significantly more than sentiment or text length. |
| **User Segments** | K-Means clustering revealed 4 distinct user archetypes: *Power Users, Active Contributors, Long-form Writers,* and *Casual Browsers*. |
| **Community Structure** | Network analysis (1,488 nodes, 132k edges) identified `user_n0ok02rt` as the ultimate structural influencer across 3 primary communities. |
| **Viral Anatomy** | Posts in the top 5% viral threshold (6,560+ engagement) surprisingly had slightly *shorter* text lengths than average posts. |

---

## Phase 2: SQL Analytics

### How to Run the SQL Queries

**Option A: Use the pre-built SQLite database (Recommended)**
1. Download `social_engine.db` from this repo
2. Open it in any SQLite tool:
   - [DB Browser for SQLite](https://sqlitebrowser.org/) (GUI, free)
   - [SQLiteStudio](https://sqlitestudio.pl/) (GUI, free)
   - Command line: `sqlite3 social_engine.db`
3. Copy and paste any query from `SQL_Queries.pdf` and run it

**Option B: Rebuild from scratch**
```bash
python phase2_sql.py
```
This loads the CSVs into a fresh SQLite database and executes all 16 queries automatically.

### Database Schema
```
users (user_id PK, location, language, account_created, follower_count)
  |
  |-- FK: user_id
  |
posts (post_id PK, user_id FK, platform, text_content, timestamp, likes, shares, comments)

posts_corrupted (same schema, raw uncleaned data for H5 anomaly detection)
```

### Phase 2 Files
| File | Description |
|------|-------------|
| `social_engine.db` | Pre-built SQLite database ready to query |
| `phase2_sql.py` | Complete Python script that creates DB and runs all 16 SQL queries |
| `SQL_Queries.pdf` | All SQL queries with schema design |
| `Logic_Explanation.pdf` | Detailed logic explanation for each query |
| `Phase2_Insight_Report.pdf` | Analytical insight report with 10 key findings |

---
*Built with cold coffee by SPARTANS*
